# 项目方法对比与风险管理报告（以增量开发为主）

## 摘要
- 目的：在对比传统与敏捷方法的基础上，将报告聚焦为“以增量开发模型（Incremental Development）为主”的实施建议，并针对本项目（校园博客，基于 `Payload` + `Next.js` + Cloudflare Workers）给出风险识别、分级与应对预案。
- 建议：以增量开发为主、结合 Kanban 做持续交付与运维，辅以明确的工程实践（CI/CD、代码审查、自动化测试、备份与安全扫描、迁移与回滚策略）。

---

## 小组分工（建议）
- 产品负责人（PO）：需求澄清、优先级、验收标准。
- 项目经理 / Scrum Master：冲刺管理、阻碍移除、风险跟踪。
- 后端工程师：Payload 配置、hooks、迁移、事务一致性。
- 前端工程师：Next.js 页面、Admin UI、性能与无障碍。
- 平台/运维（DevOps）：CI/CD、wrangler 部署、监控、备份恢复。
- 测试/QA：单元、集成、e2e 自动化与回归测试。
- 安全/合规：访问控制、依赖漏洞、合规检查。

---

## 方法比较（要点，聚焦增量开发）

### 传统模型（简要）
- 瀑布 / V 模型：适合需求和规范极其稳定、受合同或法规约束的场景，缺点在于对变更响应慢。

### 增量开发（Incremental Development）
- 定义：将系统拆为若干增量（每个增量实现一组子功能并集成到系统），逐步构建完整系统。每个增量都可单独交付、测试并与现有系统集成。
- 优点：能尽早交付可用功能、降低单次变更风险、便于用户反馈与验证；对大型系统可分阶段验证架构与接口。
- 缺点：需要良好接口设计与集成测试；若管理不当，可能导致技术债务或增量间的不兼容。

### 与敏捷（Scrum / Kanban / XP）的关系
- 增量开发强调分阶段交付，可与 Scrum 的规划与 Kanban 的持续流结合：以增量为单位制定交付里程碑，使用 Kanban 管理运维与小变更，采用 XP 工程实践保证代码质量。

---

## 对本项目的建议（以增量开发为核心，结合持续交付）
- 开发模型：以增量开发为主（每个增量交付一组完整功能或子系统），增量周期可在 1-4 周之间调整；Kanban 用于运维 / 热修复；必要时在大型变更前做短期架构冲刺。

- 增量划分建议：
  - 按功能域划分（例如：用户认证、文章发布、媒体管理、搜索/标签、评论与通知等），先交付核心可用增量（基础 CRUD 与发布流），再按优先级逐步增强功能。
  - 每个增量必须包含：API 定义、前端集成、自动化测试（单元+集成+关键 e2e）、文档与回滚脚本。

- 工程实践（强制）：
  - CI：`tsc --noEmit`、lint、单元/集成、关键 e2e、SCA（依赖扫描）。
  - PR & 代码审查：PR 模板强制 Payload 规则（Local API 使用时 `overrideAccess: false`；hooks 中嵌套调用必须传 `req`；使用 `context` 防止循环触发）。
  - 迁移策略：增量内的 DB/Schema 变更应实现向前/向后兼容；每次迁移附带回滚脚本并在 staging 验证后合并。
  - 部署：增量发布优先采用 canary 或蓝绿策略，强制 smoke tests 与自动回滚。
  - 监控与备份：错误上报、备份成功率监控、媒体配额与第三方依赖可用性报警。

---

## 风险识别与分级方法（针对增量模型补充）
- 风险评分 = 可能性（1-5）× 影响（1-5）。
  - 1-6：低，7-14：中，15-25：高。

### 增量模型下的关键风险（补充/调整）
- R1 数据丢失/损坏（评分：12-15， 中/高） — 与迁移操作或增量回滚相关。
- R2 安全漏洞 / 未授权访问（评分：10-20， 高）
- R3 第三方服务不可用（评分：9-12， 中）
- R4 部署/回滚冲突或兼容性问题（评分：10-14， 中/高） — 不同增量间接口不兼容或回滚部分增量导致功能断裂。
- R5 技术债务与累积复杂性（评分：9-15， 中到高） — 快速增量若缺少重构周期会累积技术债务。
- R6 集成测试覆盖不足导致回归（评分：9， 中）
- R7 hooks 无限循环 / 事务不一致（Payload 特有，评分：10-15， 高）

---

## 风险应对策略（增量模型下的具体措施）

### 高风险（R1、R2、R7）
- 预防：
  - 增量内变更需要明确向前/向后兼容契约；每次迁移都附带回滚脚本与恢复演练；
  - CI 中加入 SCA 与安全测试，PR 必须通过自动化检查；
  - Payload 专项：PR 模板检查 `overrideAccess`、hooks 中传 `req`、context 标志。
- 监测：异常写入检测、未授权访问告警、备份成功率告警。
- 应急：暂停相关增量的写操作→在隔离环境使用最近快照或增量快照恢复→逐步验证并合并到主线→执行 RCA。

### 中风险（R3、R4、R5、R6）
- 预防：
  - 每个增量带完整集成测试套件，强制在 staging 环境通过后才可合并；
  - 使用 canary 发布并监测关键指标，遇异常自动回滚；
  - 设立周期性重构/技术债务偿还计划（每 N 个增量后安排 1 个重构增量）。
- 应急：切换备用服务、回滚受影响增量、限流或临时降级。

---

## 详细应急模板示例（R1 数据丢失）
1. 触发：发现生产数据丢失或不一致。
2. 责任人：后端负责人 + DevOps 值班。
3. 立即动作：
   - 切系统为只读，阻止写入；
   - 在隔离环境用最近备份恢复并验证；
   - 差异比对决定全量或部分恢复策略；
   - 通知受影响方，定期更新进度。
4. 恢复后：执行 RCA，将修复纳入下个冲刺，并完善监控。

---

## 指标与治理节奏
- 推荐 KRI/KPI：备份成功率、SCA 漏洞数量、e2e 覆盖率、部署回滚次数、未授权尝试数、媒体配额使用率。
- 治理：每冲刺回顾风险状态；每月安全/灾备审查；重要变更前进行风险评审。

---

## 短期实施计划（增量模型落地，初期 4 个增量示例）
- 准备期（相当于 Sprint 0，1-2 周）：
  - 搭建 CI 基线（类型检查、lint、测试、SCA）；
  - 建立 staging 与自动备份；
  - 下发 Payload 开发规范（`overrideAccess`、`req`、`context`）；
  - 将产品功能拆分为首批 4 个增量并定义每个增量的验收标准与兼容契约（例如：核心认证与用户模型、文章发布与媒体、评论与通知、搜索/标签）。

- 首批 4 个增量（每个 1-3 周，视复杂度）：
  1. Core（用户与认证、基本文章 CRUD、Admin 接入）——交付标准：用户能发布草稿并在前端查看。
  2. 媒体管理与配额（上传、缩放、CDN 配置）——交付标准：媒体上传、自动缩放与 CDN 分发工作。
  3. 评论与通知（异步邮件/队列）——交付标准：评论发布、邮件通知或重试机制。
  4. 搜索/标签与发布流程优化——交付标准：可按标签/关键词搜索，发布工作流支持草稿/定时发布。

-- 后续：每个增量必须在 staging 完成全套集成测试、备份与回滚验证后才允许合并到主线并逐步发布。

---

## 结论与优先行动
- 结论：采用 Scrum + Kanban 混合能兼顾持续交付与运维灵活性，辅以 XP/DevOps 实践降低技术风险。
- 三项优先行动：
  1. 在 CI 中加入类型检查、测试与依赖扫描（SCA）。
  2. 建立并演练备份/恢复流程（含媒体与 DB）。
  3. 在 PR 模板中强制 Payload 校验（`overrideAccess: false`、hooks 中传 `req`、`context` 防循环）。

---

## 后续可选交付（我可以帮忙完成）
- 将本报告同步为 issue/任务板项（Sprint 0 任务）。
- 在仓库中创建 PR 模板与 CI 校验脚本（示例：Payload 检查脚本）。
- 导出为 Word/PDF 打包下载。


*生成时间：2026-05-15*
